apiVersion: v1
kind: Service
metadata:
  name: {{ include "barcode-module.fullname" . }}
  labels:
    {{- include "barcode-module.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "barcode-module.selectorLabels" . | nindent 4 }}
