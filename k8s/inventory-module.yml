apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: default
data:
  postgres_user: postgres
  postgres_password: Vasavi
  postgres_host: jdbc:postgresql://postgres-im:5432/inventory-module

---
#apiVersion: v1
#kind: PersistentVolumeClaim
#metadata:
#  name: postgres-pv-claim
#spec:
#  accessModes:
#    - ReadWriteOnce
#  resources:
#    requests:
#      storage: 1Gi
#---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-im
spec:
  selector:
    matchLabels:
      app: postgres-im
  template:
    metadata:
      labels:
        app: postgres-im
    spec:
#      volumes:
#        - name: postgres-storage
#          persistentVolumeClaim:
#            claimName: postgres-pv-claim
      containers:
        - name: postgres-im
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_user
            - name: POSTGRES_PASSWORD
              valueFrom:
                configMapKeyRef:
                  name: postgres-config
                  key: postgres_password
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
            - name: POSTGRES_DB
              value: accounting
          ports:
            - containerPort: 5432
              name: postgres-im
#          volumeMounts:
#            - name: postgres-storage
#              mountPath: /var/lib/postgresql/data
          image: postgres  
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"    
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-im
spec:
  type: ClusterIP
  ports:
    - port: 5432
  selector:
    app: postgres-im
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-module
  namespace: default
spec:
  selector:
    matchLabels: 
      app: inventory-module
  replicas: 1
  template:
    metadata:
      name: inventory-module
      labels:
        app: inventory-module
    spec:
      containers:
      - name: inventory-module
        env:
          - name: SPRING_DATASOURCE_USERNAME
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_user
          - name: SPRING_DATASOURCE_PASSWORD
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_password
          - name: SPRING_DATASOURCE_URL
            valueFrom:
              configMapKeyRef:
                name: postgres-config
                key: postgres_host
        image: 881289302514.dkr.ecr.ap-south-1.amazonaws.com/inventory-module # repository 
        resources:
          limits:
            memory: 512Mi
            cpu: "1"
          requests:
            memory: 256Mi
            cpu: "0.2"

---
apiVersion: v1
kind: Service
metadata:
  name: inventory-module
spec:
  type: LoadBalancer
  ports:
    - port: 9091
  selector:
    app: inventory-module