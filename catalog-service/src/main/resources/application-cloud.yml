server:
 port: 8085
 
spring:
 application:
    name: catalog-service
 datasource:
    platform: postgres
    url: jdbc:postgresql://10.100.198.141:5432/catalog-categories
    username: postgres
    password: Otsi@123
    #intialization-mode: always
 jpa:
   hibernate:
    ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: true
   
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://10.100.143.45:8762/eureka/
  instance:
    instance-id: ${spring.application.name}:${random.int}
    hostname: localhost

springdoc:
  packagesToScan: com.otsi.retail.catalog-service
  

  
